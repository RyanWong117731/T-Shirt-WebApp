@page
@model T_Shirt_WebApp.Pages.Transactions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Transaction[0].TShirtID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaction[0].TranactionDate)
            </th>
            <th>
                <a>User ID</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Transaction) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TShirtID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TranactionDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserAccounts.UserAccountID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TransactionID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TransactionID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TransactionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
