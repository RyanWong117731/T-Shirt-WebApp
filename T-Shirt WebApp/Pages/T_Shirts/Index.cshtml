@page
@model T_Shirt_WebApp.Pages.T_Shirts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.T_Shirt[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.T_Shirt[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.T_Shirt[0].ImagePath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.T_Shirt) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                $@Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImagePath)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.T_ShirtID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.T_ShirtID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.T_ShirtID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
